#容器内部服务
`cat temp`

```
events {
    worker_connections  1024;
}
http {
    server {
        default_type 'text/html';
        charset utf-8;
        listen 8888;
        location / {
            root /;
            index index.html;
        }
    }
}

```